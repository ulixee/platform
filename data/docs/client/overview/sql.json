{
  "content": "<h1 id=\"using-sql\"><a aria-hidden=\"true\" tabindex=\"-1\" href=\"#using-sql\">#</a>Using SQL</h1>\n<p>This Client library supports Ulixee SQL, which is a minimal subset of the ANSI SQL specification:</p>\n<pre class=\"ulixeeTheme\"><code class=\"language-javascript\">import Client from '@ulixee/client-playground';\n\nconst client = new Client('ulx://USERNAME:PASSWORD@DOMAIN:PORT/DATABASE');\nclient.query(`SELECT * FROM developers WHERE status='founder'`).then(records => {\n  console.log(records);\n});\n</code></pre>\n<p>By default all query are read-only (i.e., they must be SELECTs) unless you have special admin privileges.</p>\n<p>More details are available in the <a href=\"/docs/sql\">Ulixee SQL specification docs</a>.</p>",
  "title": "Using SQL",
  "subtitles": []
}