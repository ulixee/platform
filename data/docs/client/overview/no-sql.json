{
  "content": "<h1 id=\"using-nosql\"><a aria-hidden=\"true\" tabindex=\"-1\" href=\"#using-nosql\">#</a>Using NoSQL</h1>\n<p>Ulixee's NoSQL capabilities are still very minimal. You can do straightforward fetches, runs, and crawls, but no complicated structures such as joins are supported at this time. We recommend using Ulixee SQL for more complicated query needs.</p>\n<p>Fetch datastore tables:</p>\n<pre class=\"ulixeeTheme\"><code class=\"language-javascript\">import Client from '@ulixee/client-playground';\n\nconst client = new Client('ulx://USERNAME:PASSWORD@DOMAIN:PORT/DATABASE');\nclient.fetch('developers', { status: 'founders' }).then(records => {\n  console.log(records);\n});\n</code></pre>\n<p>Run datastore extractors:</p>\n<pre class=\"ulixeeTheme\"><code class=\"language-javascript\">import Client from '@ulixee/client-playground';\n\nconst client = new Client('ulx://USERNAME:PASSWORD@DOMAIN:PORT/DATABASE');\nclient.run('lastCommit', { package: 'hero' }).then(records => {\n  console.log(records);\n});\n</code></pre>\n<p>Crawl datastore crawlers:</p>\n<pre class=\"ulixeeTheme\"><code class=\"language-javascript\">import Client from '@ulixee/client-playground';\n\nconst client = new Client('ulx://USERNAME:PASSWORD@DOMAIN:PORT/DATABASE');\nclient.crawl('ulixee', { page: 'home' }).then(records => {\n  console.log(records);\n});\n</code></pre>",
  "title": "Using NoSQL",
  "subtitles": []
}