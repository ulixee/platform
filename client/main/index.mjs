import cjsImport from './index.js';

const {
  IInputFilter,
  IOutputSchema
  ClientForDatastore,
  ClientForTable,
  ClientForFunction,
  ClientForCrawler,
} = cjsImport;

export {
  ClientForDatastore,
  ClientForTable,
  ClientForFunction,
  ClientForCrawler,
  IInputFilter,
  IOutputSchema,
};

export default cjsImport.default;
